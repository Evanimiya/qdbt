# 리뷰: 디스패치 개발분 점검 (a 세션 → 디스패치 전달용)

> 대상: `881fa12`(T4 문서) 이후 디스패치 61커밋(HEAD `abb7ff6`). 원칙 기준 검증 결과와 액션 아이템.
> 검증 환경: create_app 부팅, tests/rob 매트릭스, data/qdbt.db 4개 입찰.

## 0. 종합 판정
**대체로 원칙에 잘 부합.** 회귀 없음, 총액 불변 유지, 테스트 하네스로 회귀 검증 확보.
단, **원칙 이탈 1건(config.py 커밋)**, **미구현 1건(T4)**, **미결 결정 1건(중간레벨 상충)**, **대량 미푸시**가 있어 아래 액션 필요.

## 1. 검증 통과 (그대로 유지 OK)
- **부팅** create_app 136 routes 정상(항목연결 라우트 +3).
- **테스트 하네스** tests/rob run_matrix **47/47 PASS · FAIL 0 · WARN 0**. (이전엔 테스트 부재 → 체계적 회귀망 확보, 매우 좋음.)
- **총액 불변 Δ=0** — 1차 테스트·1차·2차·3차 입찰 전부 렌더합=leaf합 일치(a 세션의 잎-교집합 렌더 수정 생존).
- **a 세션 작업 생존(회귀 없음)**: T3 용어(bid.html '채택' 17개), CSS 임의값 클래스(text-[10/11/12px]) 정의, 클러스터링 short-id·temperature=0·EXACT_MATCH_PREPASS(기본 OFF), 잎-교집합 매칭, stitch `_norm` 정규화.

## 2. 잘 된 신규 작업 (원칙 정합)
- **H4 도메인/통화 중립화**(7936a5f): 추출 프롬프트의 "한국 IT 입찰"·고정 5분류·KRW 하드코딩 제거 → a 세션의 런타임 도메인 폴백 중립화를 **프롬프트까지 확장**. 원칙 정확 부합. ✅
- **코드리뷰 방어 개선**: M5(토큰 32k 상향), M7(GPT content=None/finish_reason=length 처리), H5(Claude thinking/tool_use 블록 대응—text 블록만 결합), M20(합계 카테고리명 2자↑ 매칭 오탐 방지), H10(스티칭 통화 필드). 모두 타당.
- **견고성 시리즈**: seq 계층 보존, band 요약 고유비용 보존, 로케일 모호(단일점 천단위·범위표기) 처리, dedup/roll-up 손실 0·Δ=0.
- **항목연결 3단계**(코드→LLM유사제안→사람확정): 우리 "결정적→LLM→사람" 원칙과 일치. **비파괴 확인**(stitch_link.py에 submission_items 쓰기 0, 제안은 map_config JSON). ✅
- **T8 클러스터 잎 펼치기**: 표시 전용·정본 불변 준수. ✅
- **품명레벨 오라벨 버그**(c9c78ce): a 세션이 보류했던 것 수정, S9/S10 테스트로 검증.

## 3. 액션 필요 (디스패치 처리 요청)

### A. [원칙 이탈] config.py 커밋 (6094da7)
- `src/config.py`의 VERSION 0.9.4→0.9.11을 **커밋**함. 우리 규칙 "커밋 금지 대상 = src/config.py(환경 파일 보존)" 이탈.
- **실질 위험은 낮음**: config.py는 전부 env-var 구동(ANTHROPIC_API_KEY=os.environ, 경로=상대) — 머신 특정값·시크릿 없음. 그래서 VERSION 1줄 커밋은 사실상 무해.
- **요청**: (1) 정책 재확인 — config.py를 계속 커밋 금지로 둘지, 아니면 VERSION만 두는 파일로 예외 허용할지. (2) 만약 계속 금지면, 버전 표기를 별도 파일(예: `src/version.py`)로 분리 권장.

### B. [미구현] T4 되먹임 분리
- a 세션이 진단+계획+**결정까지 확정**(docs/QDBT_T4_되먹임분리_진단계획.md)했으나 디스패치는 미착수. category 되먹임 여전히 5곳(compare.py 3·clusterer.py 1·queries.py 1).
- 확정된 설계: `item_match`에 `category_override` 컬럼(additive 자동 마이그레이션), 단일 서비스 `set_item_category(item_id, cat, *, source={merge|compare})`, 읽기 경로 COALESCE(override, 정본). 확정 전=정본 편집 / 확정 후=override.
- **요청**: 착수 주체 조율(a가 이어서 할지, 디스패치가 할지). 중복 착수 방지.

### C. [미결 결정] 중간레벨(중/소/세) 부재 처리 상충 (abb7ff6)
- 직접·placeholder·복제 3방식 공존·상충. extract._prev_val은 복제(단일시트·silent), stitch는 직접. 권장 (A)직접 통일.
- **요청**: 사용자 결정 후 일괄 정리(품명 앵커 문제와 연결). 결정 전 코드 변경 보류 유지.

### D. [운영] 미푸시 82커밋
- 로컬 main이 origin/main(`f35d6b2`)보다 **82커밋** 앞섬. 원격 허브·타 머신 동기화 안 됨.
- **요청**: 사용자 허가 후 `git push origin main`(맥에서). 그 전까지 디스패치/타 세션은 origin 기준으로 오래된 코드 볼 수 있음 주의.

## 4. 권고 다음 순서
1. D(푸시)로 동기화 → 2. A(config 정책) 정리 → 3. C(중간레벨 결정) → 4. B(T4 착수 주체 확정) → 이후 T9 상태 모델.
