# QDBT 구현 종합 보고 — #2 재구성층 · #3 레벨엑셀 · #4 재레벨링 (2026-07-14)

> 목업 확정(재구성층·레벨엑셀·재레벨링). 공유 오버레이 `map_config.recon` 기반. 최신 main 위 선형 커밋.
> 원칙: 비파괴 · 정본(submission_items) 불변 · 매칭 3단계(사람=재구성) · **총액 Δ=0**(구조/표시).

## 공유 기반 — map_config.recon 오버레이 (F1)
- `get_recon/save_recon`: `{"nodes":[{id,name,level,parent_path}], "moves":{item_id:경로},
  "level_overrides":{path:레벨}}`. 정본 불변.
- `build_items_tree(submission_id, view='recon'|'original')`: moves(항목 경로 오버레이 우선)·
  nodes(빈 신규분류 삽입)·level_overrides(노드+서브트리 delta 재레벨) 반영. 금액·건수 불변.

## #4 드래그 재레벨링 (안 A: 레벨만·부모 유지)
- `POST /recon/relevel`(`_recon_relevel`): 가지 의미레벨만 변경 → `level_overrides`. **양방향**,
  **서브트리 동반**(delta), **역전 방지**(새 레벨 > 부모 레벨), **클램프**(서브트리 최대 부품6 초과 금지).
- **UI**: 캔버스 가지를 **레벨 열 헤더에 드롭** → 그 레벨로 재레벨(노드 reparent와 분리). `/recon/reset` 되돌리기.

## #3 레벨별 엑셀 추출
- `export_level_tree_xlsx` + `GET /export/level-tree.xlsx`: 열 = **대/중/소/세/품명/부품**(5레벨+부품 고정) +
  규격·수량·단위·단가·금액 + 부품수량·부품단가·파트금액. 상위 분류 셀 **반복 생략(병합 느낌)**,
  빈 레벨(placeholder 포함) **빈 칸**, **부품 행 합계 제외**, **합계=Σ품목금액**. openpyxl 스타일 재사용,
  재구성 오버레이 반영. 링크 캔버스 툴바 **⬇ 레벨 엑셀** 버튼.

## #2 신규 분류(재구성층)
- `POST /recon/node`(임의 레벨 신규 분류)·`POST /recon/move`(항목 잎/묶음 가지 오버레이 이동, 브랜치는
  하위 잎 프리픽스 교체)·`/recon/reset`.
- **UI**: 링크 캔버스 `recon:true` 모드 — 드롭이 **비파괴 오버레이 이동**(recon/move). 툴바 **＋ 신규 분류**·
  **↺ 재구성 초기화**·**👁 원본/재구성 뷰**(`?view=original`). 비교/클러스터링은 item_id 기준 무영향.

## 검증
- **견고성 매트릭스 64/64 PASS**(신규 S14 3케이스: 오버레이 이동·재레벨 클램프/역전방지·레벨엑셀).
- **실샘플 15개/27시트 스티칭 총액·건수 Δ=0**. 재구성뷰/원본뷰 총액 동일, 정본 path 불변.
- **인증 렌더**: /link 툴바(＋신규분류·초기화·원본뷰·레벨엑셀)·recon:true·`?view=original` 확인.
- 서버리스 DOM: 스코프 정렬·부품 펼쳐보기 회귀 없음. create_app **142 라우트**·Jinja compile(4템플릿)·문법 정상.

## 커밋
- `10ae178` F1 오버레이 · `eefe5d9` #4 backend · `63eaee4` #4 UI · `121769b` #3 엑셀 ·
  `e55ccf4` #2 backend · `5434105` #2/#4 UI · `8e203a7` S14+매트릭스.

## 상태 / 후속
- **부품(BOM)** 는 별도 완료(`36b7590`): 품목=집계 잎·부품=레벨6·is_header=1(총액 제외)·업체 비교 펼쳐보기.
- 실제 부품/재구성 시트 샘플 확보 시 자동감지·그루핑 재조정 여지(현재 표준구조 기준).
- 전체 견고성 64/64·Δ=0. VDI 패치 묶음 준비 완료(버전 bump 필요 시 안내).
