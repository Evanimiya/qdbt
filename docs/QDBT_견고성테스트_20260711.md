# QDBT 견고성 테스트 — 엉망 견적서 파이프라인 (2026-07-11)

> '엉망으로 접수된' 견적서 시나리오를 합성 워크북(openpyxl)으로 만들어 `extract_by_mapping` · 스티칭(`stitch`) · 통화정합(`apply_currency_fields`) · 집계(`recompute_subtotal` 미러)를 **함수 직접 호출**로 검증. 포그라운드 서버 없음.

**결과: 44/44 PASS · FAIL 0 · WARN 0.** 1차 발견 버그 5건(합계행 미탐 이중계상, seq모드 leaf 소실, band 고유비용 소실, 시트간 동일항목 미표기, 로케일 모호)을 모두 Δ=0 게이트로 수정 — 아래 §발견·수정 참조.

## 재현

```bash
./venv/bin/python tests/rob/run_matrix.py   # 그룹모듈형(S1~S7, 상위집합)
./venv/bin/python tests/rob/run_all.py      # 동반 단일파일 하네스
```

픽스처·결과는 재생성물(`tests/rob/_fixtures/`, `_results.jsonl`; .gitignore). 하네스 모듈: `tests/rob/s1..s7_*.py`, 공용 유틸 `_util.py`.

## 집계 미러 근거

`leaf_total`(하네스) = 스티칭 `totals.leaf_sum` = `Σ amount` (`merge_status`·`is_category_header` 제외). `insert_items_bulk` 는 그 두 플래그를 `is_header=1` 로 저장하고, `recompute_subtotal` 은 `get_items(headers=False)` (=`is_header=0`) 합을 쓰므로 **저장 후 집계 총액과 동일**. 통화정합은 `amount` 에 이미 반영(원화 확정).

## S1 분류끊김  (5/5 PASS)

| ID | 케이스 | 입력 요지 | 기대 | 실제 | 판정 | 근본원인/비고 |
|---|---|---|---|---|---|---|
| 1a | 비연속 레벨(중+세, 소 skip) | cat2,cat4 매핑 · 세분류가 소분류 자리로 당겨지면 안 됨 | 세분류 depth4 보존, 대·소 placeholder, residual, 총액300 | depth=4 path=⟨미연계·대분류⟩ > 기구부 > ⟨미연계·소분류⟩ > 차폐 resid=True tot=300.0 | ✅ PASS | — |
| 1b | 대분류 없이 중분류부터 시작 | 중분류가 대분류로 승격되면 안 됨(placeholder로 자리 보존) | depth3, 대분류 placeholder, 중=전장부, residual | depth=3 path=⟨미연계·대분류⟩ > 전장부 > 제어 resid=True | ✅ PASS | — |
| 1c | 행마다 depth 불일치 | 각 행 depth 달라도 잎 3건·총액600 보존, 재료비 하위 유지 | 3건, 총액600, 모두 재료비 루트 | n=3 names={'일반자재', '납블록', '순납'} tot=600.0 | ✅ PASS | — |
| 1d | 중간 빈 분류 셀 fill-down | 빈 분류 셀이 위 행에서 상속되어야 | 브래킷=재료비>기구부, PLC=재료비>전장부, 총액600 | paths={'납블록': '재료비 > 기구부', '브래킷': '재료비 > 기구부', 'PLC': '재료비 > 전장부'} tot=600.0 | ✅ PASS | — |
| 1e | 항목 연결 3단계 원칙(완전합치=코드, 유사도=LLM 위임) [Fix3] | 시트 간 동일 품명·규격·금액 — 유사(다른경로) vs 완전동일(같은경로) | 완전동일만 코드 dedup, 유사는 별도 잎 보존(총액 불변)·코드종결 없이 LLM에 위임 | 유사:kept=2 총액=10000 종결표기=0/False \| 완전동일:dedup=1 총액=5000 | ✅ PASS | — |

## S2 다중통화  (5/5 PASS)

| ID | 케이스 | 입력 요지 | 기대 | 실제 | 판정 | 근본원인/비고 |
|---|---|---|---|---|---|---|
| 2-sym | 통화기호/명칭 정규화(¥€₩$元 등) | 12종 기호·명칭 입력 | ¥→CNY(도메인관례), $→USD, ₩/원/공란→KRW, 元→CNY, 엔→JPY | 불일치: 없음 | ✅ PASS | — |
| 2a | USD+KRW+JPY 혼재(원화금액 열 존재) | 각 행 원화확정, 통화배수 오류 없이 총액 정합 | 총액 149,000 · USD행 amount=130000(orig 100) | 총액=149000.0 · USD amount=130000 orig=100 | ✅ PASS | — |
| 2c | 환율 도출 실패 외화(원화열 부재) | 외화 표면값이 KRW 총액에 혼입되면 안 됨(H3) | 총액 10,000 · USD행 amount=None · needs_fx=True | 총액=10000.0 · USD amount=None needs_fx=True | ✅ PASS | — |
| 2d | 원화단가로 환율 역산 → 원화금액 확정 | fx=1300 역산, 단가·금액 원화확정, amount=단가×수량 정합 | fx=1300.0 단가=130000 금액=1300000 라인정합=True | fx=1300.0 단가=130000 금액=1300000 | ✅ PASS | — |
| 2e | 다중시트 통화 정합(스티칭) | 스티칭 경로도 원화 확정 → 총액 통화배수 오류 없음 | 총액 150,000 · 수입칩 amount=130000 | 총액=150000 · 칩 amount=130000 | ✅ PASS | — |

## S3 다중탭  (5/5 PASS)

| ID | 케이스 | 입력 요지 | 기대 | 실제 | 판정 | 근본원인/비고 |
|---|---|---|---|---|---|---|
| 3a | 갑지(요약)↔상세 roll-up | 요약 3000+5000 이 상세 카테고리합과 일치 → 요약 제외 | 총액 8,000(상세 잎만) · roll-up 2건 | 총액=8000 rollups=2 drop=2 | ✅ PASS | — |
| 3b | 요약 부분 불일치 → 미매칭 요약 residual | 갑지 재료비=상세합(roll-up), 부대비=상세에 없음 | 부대비는 summary_unmatched residual로 보존(사람 확인) | reasons={'summary_unmatched'} unmatched=['부대비'] | ✅ PASS | — |
| 3c | 겹치는 밴드 파노라마 조인 | 골격1[대>중]·골격2[중>소]·명세[소>품명] → 대>중>소 복원 | path=컴퓨팅>GPU컴퓨팅>GPU서버, name=A100, 총액1000 | kept=1 path=컴퓨팅 > GPU컴퓨팅 > GPU서버 tot=1000 | ✅ PASS | — |
| 3e | 선택 시트만 추출(미선택·설명 혼입 방지) | 상세만 선택 → 갑지요약(99999)·설명 시트 혼입 금지 | 잎={볼트}, 총액 1,000 | names={'볼트'} tot=1000 | ✅ PASS | — |
| 3f | '_' 접두 시트 자동 제외 | _메모 시트는 스티칭 대상에서 제외 | 대상 시트에 _메모 없음 | used={'상세', '갑지요약', '설명'} | ✅ PASS | — |

## S4 번호체계  (4/4 PASS)

| ID | 케이스 | 입력 요지 | 기대 | 실제 | 판정 | 근본원인/비고 |
|---|---|---|---|---|---|---|
| 4a | 번호 구분자 무관 파싱(점·하이픈·공백·가운뎃점·혼합) | 13종 번호 표기 | 숫자그룹 튜플화, 치수(1-1/2인치)·비번호는 None | 불일치: 없음 | ✅ PASS | — |
| 4b | 정수 No + 분류열 → 계층 보존(seq 모드 미발동) | No가 seq로 매핑돼도 분류 계층을 버리면 안 됨(BUG A) | 볼트=재료비>기구부, PLC=재료비>전장부, 총액600 | paths={'볼트': '재료비 > 기구부', 'PLC': '재료비 > 전장부', '설치공': '노무비 > 설치'} tot=600.0 | ✅ PASS | — |
| 4c | 중복 번호(같은 1.1 두 행) | 중복 번호라도 다른 품명이면 둘 다 보존(손실·병합 없음) | 볼트·너트 각 1건, 재료비 하위, 총액300 | names=['볼트', '너트'] tot=300.0 | ✅ PASS | — |
| 4d | 시트 간 동일 No(세부시트 2개) | 세부시트가 같은 번호를 써도 각각 보존(다중 seq_leaf) | 볼트·케이블 둘 다, 총액300 | kept=2 names={'볼트', '케이블'} tot=300 | ✅ PASS | — |

## S5 금액이상치  (7/7 PASS)

| ID | 케이스 | 입력 요지 | 기대 | 실제 | 판정 | 근본원인/비고 |
|---|---|---|---|---|---|---|
| 5-num | 금액 파싱(괄호·△▲−·콤마·통화기호·유럽식) | 16종 표기 | 부호 보존·천단위 제거·통화기호 제거·빈값 None | 불일치: 없음 | ✅ PASS | — |
| 5-fp | 합계 오탐 방지 | 품명에 우연히 '계/합계/total' 포함 | 모두 합계행 아님(False) | 오탐: 없음 | ✅ PASS | — |
| 5-fn | 합계 미탐 방지 | 실제 합계/소계/CJK/영문 합계행 | 모두 합계행(True) | 미탐: 없음 | ✅ PASS | — |
| 5-dc | 합계/소계행 혼입 이중계상 방지(스티칭) | 잎(볼트1000+너트2000) + 소계3000 + 합계3000 혼입 | 총액 3,000(합계·소계 제외) | 총액=3000 · 잎={'너트', '볼트'} | ✅ PASS | — |
| 5-det | detect_total_rows 합계행 제안 | 소계·합계 행 존재 | 둘 다 제외 후보로 제안 | 제안={'소계', '합계'} | ✅ PASS | — |
| 5-neg | 괄호·삼각 음수 차감 반영 | 10000 + (2,000) + △1,000 | 할인=-2000, 에누리=-1000, 총액 7,000 | amounts={'기본공급': 10000, '할인': -2000, '에누리': -1000} 총액=7000.0 | ✅ PASS | — |
| 5-empty | 빈 금액/단가 행 | 정상행 1000 + 금액·단가 빈 행 | 총액 1,000(빈 행 0 기여, 크래시 없음) | 총액=1000.0 n=2 | ✅ PASS | — |

## S6 기타  (7/7 PASS)

| ID | 케이스 | 입력 요지 | 기대 | 실제 | 판정 | 근본원인/비고 |
|---|---|---|---|---|---|---|
| 6a | 품명 미지정(분류만) 추출 | 품명 열 부재 → 최말단 분류값을 잎으로(추출 0 방지) | 볼트류·너트류 2건, 총액3000 | names={'너트류', '볼트류'} n=2 tot=3000.0 | ✅ PASS | — |
| 6b | 완전중복 항목(시트 간) dedup | 동일 분류경로+품명+규격+금액 → 1건 정본, 나머지 제외(총액 불변) | 총액 1,000 · duplicates 1 · 잎 1건 | 총액=1000 dup=1 n=1 | ✅ PASS | — |
| 6c | seq 다중 세부시트 모두 보존 | 세부A·세부B 서로 다른 세부시트 → 통째 소실 없이 모두 처리 | 볼트·설치공 보존, 총액300 | names={'볼트', '설치공'} tot=300 | ✅ PASS | — |
| 6d | 병합셀(대분류 세로병합) 복원 | A2:A4 병합 → 너트·와셔도 재료비 상속 | 3건 모두 재료비, 총액600 | paths=['재료비', '재료비', '재료비'] tot=600.0 | ✅ PASS | — |
| 6e | 헤더 위치 이상(제목행 위) | 제목·빈행 위 → header_row=3, 자동탐지도 3 | 2건 총액300, suggest header_row=3 | n=2 tot=300.0 suggest_hr=3 | ✅ PASS | — |
| 6f | seq모드 leaf 독립 breakdown 보존 | 갑지(정수No+금액 role=leaf) + 산출(1.1 seq_leaf), 갑지는 독립(총액 불일치) | 갑지 잎+residual 보존, 총액 8,300(은닉 소실 0) | 총액=8300 · 갑지흔적=보존 | ✅ PASS | — |
| 6f-2 | seq모드 요약 leaf → 전체요약 roll-up | 갑지 총액(300)=산출 정본 총액 → 요약 판별 | 총액 300(갑지 roll-up 제외, 이중계상 없음) | 총액=300 rollups=2 | ✅ PASS | — |

## S7 경계탐침  (3/3 PASS)

| ID | 케이스 | 입력 요지 | 기대 | 실제 | 판정 | 근본원인/비고 |
|---|---|---|---|---|---|---|
| 7-1 | 단일점 천단위 휴리스틱 '1.000' | '1.000'(3자리 소수부) · '3.5' · '0.125' | '1.000'→1000(천단위), '3.5'→3.5·'0.125'→0.125(소수 유지) | 1.000=1000 3.5=3.5 0.125=0.125 | ✅ PASS | — |
| 7-4 | band 요약 고유비용(부대비) 보존 [Fix2] | 갑지(band, 대분류별 금액)에 상세에 없는 부대비 500 | 부대비 잎+residual 보존, 총액 3,500(누락 없음) | 총액=3500 · 부대비=보존 | ✅ PASS | — |
| 7-5 | 범위표기 '1-5' 미상 보존 | 금액열에 '1-5'/'1~5' 범위 문자열 | 억지 숫자화(15) 대신 None(미상) 보존 → 오계상 방지 | '1-5'=None '1~5'=None | ✅ PASS | — |

## S8 항목연결3단계  (4/4 PASS)

| ID | 케이스 | 입력 요지 | 기대 | 실제 | 판정 | 근본원인/비고 |
|---|---|---|---|---|---|---|
| 8-1 | 1단계 완전합치=dedup / 유사=후보(비파괴) | 같은 경로 동일항목 vs 다른 경로 유사항목 | 완전동일 dedup·후보0 / 유사 후보1·자동병합0·총액10000(Δ=0) | 완전동일:dup=1,후보=0 \| 유사:후보=1,dup=0,총액=10000.0 | ✅ PASS | — |
| 8-2 | 2단계 LLM 유사 제안(same=true/false) | 유사 후보를 LLM이 동일 라인아이템인지 판정 | same=true→proposed 1건, same=false→제안 0(자동병합 없음) | true:llm/1/proposed \| false:0 | ✅ PASS | — |
| 8-3 | 폐쇄망 휴리스틱 폴백(proposed/pending) | LLM 미가용 → 결정적 휴리스틱, 애매하면 pending | 규격·금액 일치→proposed, 규격 불명→pending(사람 확인 대기) | 규격일치:proposed(score 0.85) \| 규격불명:pending | ✅ PASS | — |
| 8-4 | 3단계 사람 확정(accept Δ=0 / 명시 제외 가역) | 제안 accept / 사람 명시 중복 제외·복원 | accept 총액 불변(10000), 제외 5000·복원 10000(되돌리기 가능) | tot0=10000.0 accept=10000.0 exclude=5000.0 restore=10000.0 | ✅ PASS | — |

## S9 품명레벨  (4/4 PASS)

| ID | 케이스 | 입력 요지 | 기대 | 실제 | 판정 | 근본원인/비고 |
|---|---|---|---|---|---|---|
| 9-1 | [대,중,품명] extract 구조 | cat1+cat2+name 매핑(소분류 없음) | path=재료비>기구부(depth2), 품명=납블록 잎, placeholder 없음 | path=재료비 > 기구부 depth=2 name=납블록 resid=None | ✅ PASS | — |
| 9-2 | [대,중,품명] stitch 구조 | 스티칭 경로도 품명을 잎으로 | path=재료비>기구부, 품명=납블록 잎 | path=재료비 > 기구부 name=납블록 | ✅ PASS | — |
| 9-3 | build_items_tree 품명 잎 판정 | 트리에서 품명 노드 | 납블록 is_leaf=True (재료비>기구부 아래 잎) | 납블록: leaf=True depth=3 path=재료비 > 기구부 > 납블록 | ✅ PASS | — |
| 9-4 | 연계 캔버스 열 헤더 라벨(node DOM) | [대,중,품명]→대\\|중\\|품명, [대,중,소,세,품명]→회귀 없음 | 품명 잎 열이 '품명'으로 라벨(소분류 아님) | ✅ PASS 품명레벨 라벨 (버그 수정 확인) | ✅ PASS | — |

## 발견된 버그 · 수정 · 결정필요

### ✅ 수정 (Δ=0 게이트 통과)

- **합계행 미탐 → 이중계상 (S5 5-dc / 5-det)**: 합계 마커가 품명 셀에 있고 분류 셀 값이 조인 문자열 뒤에 붙는 경우(`"소계 재료비"`) `is_total_label` 의 `endswith` 앵커가 무력화돼 소계·합계 행이 잎으로 계상(이중계상)됐다. `_is_total_row`(stitch)·`detect_total_rows`(extract) 를 조인 검사 + **셀 단독 검사**로 보강. 실샘플 **15개 / 31시트 / 3446행** 에서 per-cell 신규 발동 **0건 → Δ=0** 확인 후 반영. (병합된 대분류가 소계행에 채워지는 흔한 양식에서 발생.)

- **seq 모드 leaf/breakdown 시트 은닉 소실 (S6 6f) — 해소**: 정수번호+금액 시트(role `leaf`, 예 갑지·공종목록)가 점계층 세부와 함께 올 때 `_stitch_seq` 가 `seq_*` 만 방출해 leaf 시트가 흔적 없이 소실됐다. → leaf 시트도 방출하고, 요약 판별을 **전체요약 roll-up**(시트 총액 = 상세 정본 총액이면 전 행 roll-up)으로 보강. 독립 breakdown은 residual 보존(손실 0), 요약은 roll-up(이중계상 방지). 실샘플 C3 `공종목록`(대분류 요약)이 상세 상위카테고리(더 깊은 레벨)와 이름이 안 맞아도 총액 일치로 roll-up → **C3 Δ=0**. (S6 6f/6f-2 PASS)

- **band 요약 고유비용 소실 (S7 7-4) — 해소**: band 요약 행 중 '완전 고립'(분류값이 어떤 잎 경로에도 미등장, 예 부대비)만 선별해 잎+residual로 보존→총액 누락 방지. 한 값이라도 잎과 공유하면 계층 일부(빈 카테고리 소계)로 보고 미방출 → **C2 Δ=0**(모든 band 행 커버 시 무방출). band 시트 간 동일경로 중복 방출도 차단. (S7 7-4 PASS)

- **시트 간 동일항목·분류명만 상이 (S1 1e) — 해소 (항목 연결 3단계 원칙)**: `1) 완전합치(코드)`=정규화 경로+품명+규격+금액 정확일치만 자동 dedup. `2) 유사도(LLM)`=완전합치 안 된 유사항목은 스티칭(무LLM)이 병합·미연계 종결하지 않고 별도 정본 잎으로 보존(총액 Δ=0)해 DB 저장 → 기존 `catalog_clusterer`(코드 exact prepass→LLM 유사도 클러스터)·`matcher`(LLM 카탈로그, `suggested`)가 연결 **제안**. `3) 사람 점검`=담당자 accept/reject 확정. 코드-종결 `cross_sheet_similar` 플래그는 2단계(LLM)를 건너뛴 오종결이라 **제거**. 원칙을 `stitch.py` 배너 주석·CHANGELOG에 명시. (S1 1e PASS)

- **로케일 모호 파싱 (S7 7-1 '1.000', 7-5 '1-5') — 해소**: 단일 점 + 정확히 3자리 소수부 → 유럽식 천단위(`'1.000'→1000`; `'3.5'`·`'0.125'` 소수 유지). `'1-5'`/`'1~5'` 범위표기는 억지 숫자화(15) 대신 `None`(미상) 보존해 오계상 방지 (계층번호는 seq 역할 열의 `seq_tuple` 이 별도 처리). 실샘플 총액 Δ=0. (S7 7-1/7-5 PASS)

### 🔗 스티칭 자체 3단계 항목 연결 (S8 — 구현)

- 스티칭이 **자체적으로** 항목 연결 3단계를 수행(intra-submission 시트 간): `①완전합치(코드)` 정확일치만 자동 dedup → `②유사 합치(LLM 제안)` 완전합치 안 된 유사 후보(`stitch._detect_link_candidates`: 정규화 품명 동일+다른 경로+다른 시트+금액 근접)를 `stitch_link.suggest_links`가 LLM으로 '동일 라인아이템?' 판정해 **제안**(자동 병합 없음) → `③사람 확정` `POST /links/confirm` accept(연결·**금액 불변**)/reject/명시적 중복 제외(되돌리기 가능·이때만 총액 변동). **폐쇄망/무LLM**: 결정적 휴리스틱 폴백 → 애매하면 `pending`(사람 확인 대기). 제안·확정은 전부 `map_config`(정본 `submission_items` 불변). 코드↔DB↔라우트↔UI 경로 검증(temp DB·인증 렌더) 완료. (S8 8-1~8-4 PASS)

### ⚠️ 남은 결정 필요

- **없음.** 상정한 엉망 시나리오(S1~S7)는 합리적 기본값으로 모두 처리됨. 위 로케일 휴리스틱(단일점3자리=천단위)은 도메인(KRW 정수 우세) 기본값으로, 3자리 소수 수량이 흔한 타 도메인 도입 시 통화/로케일 힌트로 재검토 여지가 있다(관찰 사항, 현재 정본 로직 회귀 없음).

## 통화 정합 특기

- 외화인데 원화열·환율 도출이 모두 불가하면 `amount=None`+`needs_fx=True` 로 격리 → **KRW 총액에 외화 표면값 미혼입**(통화배수 오류 없음, S2 2c). 다중시트 스티칭도 동일 규칙(2e).

- `¥` 는 엔·위안 공용 기호라 도메인 관례상 **CNY** 로 정규화(명시 코드 우선) — 표시 유의(S2 2-sym).

